const db = require( './../db/index');
/*
export default class QuestionModel {
    async find_by_answer(answer_id) {
        try{
            const queryText = `
            `;
            const result = await db.query(queryText, [answer_id]);
            return result.rows;
        }catch(err){
            throw err;
        }
    }
};*/
